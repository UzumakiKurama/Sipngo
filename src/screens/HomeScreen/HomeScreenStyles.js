import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    main:{
        flex:1,
        height : "100%"
    },
    listContainer : {
        flex : 1,
        height: '100%',
        margin : 4,
    }
})

export default styles; 